import { RouteLoader } from "../../components/route-loader";

export default function Loading() {
  return <RouteLoader title="Target Detail" subtitle="Loading target authorization..." variant="detail" />;
}
