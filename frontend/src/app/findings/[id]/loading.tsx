import { RouteLoader } from "../../components/route-loader";

export default function Loading() {
  return <RouteLoader title="Finding Detail" subtitle="Loading finding evidence..." variant="detail" />;
}
